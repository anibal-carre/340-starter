<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>
        <%= title %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        /* Simple responsive layout meeting assignment requirements */
        :root {
            --gap: 1.25rem;
            --max-width: 1100px;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
            margin: 0;
            padding: 1rem;
            background: #f7f7f7;
            color: #222;
        }

        .container {
            max-width: var(--max-width);
            margin: 0 auto;
            background: #fff;
            padding: 1rem;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
            border-radius: 8px;
        }

        .grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: var(--gap);
            align-items: start;
        }

        .vehicle-image {
            width: 100%;
            height: auto;
            object-fit: cover;
            border-radius: 6px;
        }

        .content h1 {
            margin: 0 0 .5rem 0;
            font-size: 1.6rem;
        }

        .meta {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
            margin-bottom: 0.75rem;
            align-items: baseline;
        }

        .price {
            font-size: 1.25rem;
            font-weight: 700;
            color: #0b6;
        }

        .mileage {
            color: #666;
        }

        .description {
            margin-top: .75rem;
            line-height: 1.5;
        }

        /* Side-by-side on larger screens */
        @media (min-width: 900px) {
            .grid {
                grid-template-columns: 1fr 1fr;
            }
        }

        /* Accessibility and readability */
        .label {
            font-weight: 600;
        }

        .specs {
            margin-top: .5rem;
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .spec {
            background: #f1f1f1;
            padding: .4rem .6rem;
            border-radius: 6px;
            font-size: .9rem;
        }

        /* keep image from overflowing on small screens */
        .img-wrap {
            width: 100%;
        }
    </style>
</head>

<body>
    <div class="container" role="main">
        <div class="grid" aria-labelledby="vehicle-title">
            <div class="img-wrap">
                <img src="<%= vehicle.imagePath %>" alt="<%= vehicle.year %> <%= vehicle.make %> <%= vehicle.model %>"
                    class="vehicle-image" />
            </div>

            <div class="content">
                <h1 id="vehicle-title">
                    <%= vehicle.year %>
                        <%= vehicle.make %>
                            <%= vehicle.model %>
                </h1>

                <div class="meta" role="region" aria-label="price and mileage">
                    <div class="price" aria-live="polite">
                        <%= vehicle.price %>
                    </div>
                    <div class="mileage"><span class="label">Mileage:</span>
                        <%= vehicle.mileage %> miles
                    </div>
                </div>

                <div class="specs" aria-label="vehicle specifications">
                    <div class="spec"><strong>Make:</strong>
                        <%= vehicle.make %>
                    </div>
                    <div class="spec"><strong>Model:</strong>
                        <%= vehicle.model %>
                    </div>
                    <div class="spec"><strong>Year:</strong>
                        <%= vehicle.year %>
                    </div>
                    <div class="spec"><strong>ID:</strong>
                        <%= vehicle.inv_id %>
                    </div>
                </div>

                <div class="description">
                    <h2>Description</h2>
                    <p>
                        <%= vehicle.description || 'No description available.' %>
                    </p>
                </div>

                <% /* If you also want to insert the utility-built HTML snippet */ %>
                    <div class="utility-html">
                        <%- detailHTML %>
                            <!-- note: detailHTML was escaped earlier, we used buildVehicleDetailHTML which is safe -->
                    </div>
            </div>
        </div>
    </div>
</body>

</html>