<main>
    <% if (title) { %>
        <h1>
            <%= title %>
        </h1>
        <% } %>

            <% if (grid && grid.trim() !=="" ) { %>
                <%- grid %>
                    <% } else { %>
                        <p class="notice">No vehicles found for this classification.</p>
                        <% } %>
</main>